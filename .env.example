# Server-only MongoDB connection string for the crd database.
MONGODB_URI=

# Server-only secret used to sign admin sessions.
ADMIN_JWT_SECRET=

# Random server-only secret that authorizes the daily database heartbeat.
CRON_SECRET=
