/* API-backed directory with resilient DOM access and URL-based comparison handoff. */
(()=>{const existing=window.CredwisoryLenderDirectory;const directory=existing||(()=>{const fields=[['Secured loan maximum','secured'],['Unsecured loan maximum','unsecured'],['Secured interest rate','securedRate'],['Unsecured interest rate','unsecuredRate'],['Moratorium period','moratorium'],['Loan tenure','tenure'],['Foreclosure charges','foreclosure'],['Processing fees','fee']],connected=root=>root?.isConnected===true,category=value=>{const valueLower=String(value||'').toLowerCase();return ['bank','nbfc','international'].includes(valueLower)?valueLower:null},initials=name=>name.split(/\s+/).map(part=>part[0]).join('').slice(0,4).toUpperCase(),detail=(lender,key,fallback='Contact for details')=>String(lender.comparison?.[key]??lender[key]??fallback),display=value=>value==='Not available'?'<span class="lc-na">Not available</span>':value,normalize=lender=>{const type=category(lender.lenderType);return type?{id:String(lender.slug||lender._id),type,initials:initials(lender.name),name:lender.name,secured:detail(lender,'secured'),unsecured:detail(lender,'unsecured'),securedRate:detail(lender,'securedRate',lender.securedRate||'Contact for details'),unsecuredRate:detail(lender,'unsecuredRate',lender.unsecuredRate||'Contact for details'),moratorium:detail(lender,'moratorium'),tenure:detail(lender,'tenure',lender.tenure||'Contact for details'),foreclosure:detail(lender,'foreclosure'),fee:detail(lender,'fee',lender.processingFee||'Contact for details')}:null};
const idsFromUrl=items=>{const requested=new URLSearchParams(location.search).get('compare')||'';const valid=new Set(items.map(item=>item.id));return [...new Set(requested.split(',').map(value=>value.trim()).filter(value=>valid.has(value)))].slice(0,4)};const markCompareScroll=ids=>{try{sessionStorage.setItem('cw-lender-compare-scroll',ids.join(','))}catch{}};
function render(root,lenders){if(!connected(root))return;const kind=root.dataset.lenderCategory||'all',comparePage=kind==='all',items=comparePage?lenders:lenders.filter(lender=>lender.type===kind);if(!items.length){root.innerHTML=`<p class="lc-empty" role="status">No published ${comparePage?'lenders':kind+' lenders'} are available right now.</p>`;return}let selected=comparePage?idsFromUrl(items):[];root.innerHTML=`<div class="lc-toolbar"><div><p class="lc-count" aria-live="polite"><span>0</span> of 4 lenders selected</p><p class="lc-selection-message" role="status" aria-live="polite" hidden></p></div><button class="lc-compare" type="button" disabled>${comparePage?'Compare selected lenders':'Compare selected lenders'}</button></div><div class="lc-scroll" tabindex="0" aria-label="Scrollable list of education loan lenders"><table><thead><tr><th>Lender</th>${fields.map(([label])=>`<th>${label}</th>`).join('')}<th>Compare</th><th>Action</th></tr></thead><tbody></tbody></table></div><div class="lc-hint" aria-hidden="true">Scroll to view all lenders</div>${comparePage?'<section class="lc-inline" hidden aria-live="polite"><div class="lc-inline-head"><div><p class="lc-eyebrow">SIDE-BY-SIDE VIEW</p><h2>Your selected lender comparison</h2></div><button class="lc-clear" type="button">Clear comparison</button></div><div class="lc-grid-wrap"><div class="lc-grid"></div></div></section>':''}`;
const draw=()=>{if(!connected(root))return false;const tbody=root.querySelector('tbody');if(!tbody)return false;const full=selected.length===4;tbody.innerHTML=items.map(lender=>`<tr><td data-label="Lender"><div class="lc-lender"><span class="lc-badge">${lender.initials}</span><span><b>${lender.name}</b><small>${lender.type==='international'?'International lender':'Illustrative'}</small></span></div></td>${fields.map(([label,key])=>`<td data-label="${label}">${display(lender[key])}</td>`).join('')}<td data-label="Compare"><label class="lc-choice"><input type="checkbox" data-id="${lender.id}" ${selected.includes(lender.id)?'checked':''} ${full&&!selected.includes(lender.id)?'disabled':''}> Compare</label></td><td data-label="Action"><a class="lc-apply" href="lender-enquiry.html?lender=${encodeURIComponent(lender.id)}">Apply with us →</a></td></tr>`).join('');const count=root.querySelector('.lc-count'),button=root.querySelector('.lc-compare'),message=root.querySelector('.lc-selection-message');if(count)count.innerHTML=`<span>${selected.length}</span> of 4 lenders selected`;if(button)button.disabled=selected.length<2;if(message){message.hidden=!full;if(full)message.textContent='Maximum reached: deselect one lender to choose another.'}return true};
const showComparison=scroll=>{if(!comparePage||!connected(root))return;const inline=root.querySelector('.lc-inline'),grid=root.querySelector('.lc-grid');if(!inline||!grid||selected.length<2)return;const chosen=selected.map(id=>items.find(lender=>lender.id===id)).filter(Boolean);grid.innerHTML=chosen.map(lender=>`<article class="lc-summary"><header><span class="lc-badge">${lender.initials}</span><b>${lender.name}</b></header><button class="lc-remove" type="button" data-remove="${lender.id}">Remove</button><dl>${fields.map(([label,key])=>`<div><dt>${label}</dt><dd>${display(lender[key])}</dd></div>`).join('')}</dl></article>`).join('');grid.style.setProperty('--lc-cols',chosen.length);inline.hidden=false;if(scroll)inline.scrollIntoView({behavior:'smooth',block:'start'})};
if(!draw())return;root.addEventListener('change',event=>{if(!connected(root))return;const input=event.target.closest('[data-id]');if(!input)return;selected=input.checked?[...selected,input.dataset.id]:selected.filter(id=>id!==input.dataset.id);draw()});root.querySelector('.lc-compare')?.addEventListener('click',()=>{if(selected.length<2)return;if(comparePage){showComparison(true);return}markCompareScroll(selected);location.href=`compare-all-lenders.html?compare=${encodeURIComponent(selected.join(','))}`});root.querySelector('.lc-clear')?.addEventListener('click',()=>{selected=[];const inline=root.querySelector('.lc-inline');if(inline)inline.hidden=true;draw()});root.addEventListener('click',event=>{const remove=event.target.closest('[data-remove]');if(!remove||!connected(root))return;selected=selected.filter(id=>id!==remove.dataset.remove);const inline=root.querySelector('.lc-inline');if(inline)inline.hidden=true;draw()});if(comparePage&&selected.length>=2){let scroll=false;try{const key=sessionStorage.getItem('cw-lender-compare-scroll');scroll=key===selected.join(',');if(scroll)sessionStorage.removeItem('cw-lender-compare-scroll')}catch{}showComparison(scroll)}}
async function mount(root){if(!connected(root)||root.dataset.lenderDirectoryMounted==='true')return;root.dataset.lenderDirectoryMounted='true';root.innerHTML='<p class="lc-loading" role="status" aria-live="polite">Loading lenders…</p>';try{const response=await fetch('/api/lenders',{headers:{accept:'application/json'}});if(!response.ok)throw new Error();const payload=await response.json();if(!Array.isArray(payload))throw new Error();if(connected(root))render(root,payload.map(normalize).filter(Boolean))}catch{if(connected(root))root.innerHTML='<p class="lc-error" role="alert">We could not load lenders right now. Please refresh the page or try again shortly.</p>'}}return{mount,initialize:()=>document.querySelectorAll('[data-lender-category]').forEach(mount)}})();window.CredwisoryLenderDirectory=directory;const start=()=>directory.initialize();if(document.readyState==='loading'){if(!window.__cwLenderDirectoryListening){window.__cwLenderDirectoryListening=true;document.addEventListener('DOMContentLoaded',start,{once:true})}}else start();if(!document.getElementById('cw-lender-directory-states')){const style=document.createElement('style');style.id='cw-lender-directory-states';style.textContent='.lc-loading,.lc-error,.lc-empty{margin:0;padding:24px;color:#61718a;font-weight:700}.lc-error{color:#9a6b00}.lc-selection-message{max-width:430px;margin:5px 0 0;color:#9a6b00;font-size:.82rem;font-weight:700}@media(max-width:700px){.lc-scroll{height:min(68vh,560px);overflow-y:auto;overflow-x:hidden}.lc-scroll table,.lc-scroll tbody{display:block;min-width:0}.lc-scroll thead{display:none}.lc-scroll tr{display:block;margin:12px;border:1px solid var(--l);border-radius:14px;overflow:hidden}.lc-scroll td{display:grid;grid-template-columns:minmax(124px,44%) minmax(0,1fr);gap:10px;padding:11px 13px;word-break:break-word}.lc-scroll td:first-child{padding:14px 13px;background:#f7fbfc}.lc-scroll td::before{content:attr(data-label);color:#61718a;font-size:.72rem;font-weight:800;text-transform:uppercase}.lc-scroll td:first-child::before{display:none}.lc-scroll td:last-child{border-bottom:0}.lc-lender{min-width:0}.lc-apply{justify-self:start}}';document.head.append(style)}})();
