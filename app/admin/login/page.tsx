"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import Image from "next/image";
export default function LoginPage() { const [error, setError] = useState(""); const [busy, setBusy] = useState(false); const router = useRouter(); async function login(form: FormData) { setBusy(true); setError(""); try { const response = await fetch("/api/admin/auth/login", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ email: form.get("email"), password: form.get("password") }) }); const data = await response.json().catch(() => null); if (response.ok) { router.replace(data?.passwordChangeRequired ? "/admin/change-password" : "/admin"); router.refresh(); } else setError(data?.error || "Unable to sign in. Check your credentials."); } catch { setError("Sign-in is temporarily unavailable. Please retry."); } finally { setBusy(false); } } return <main className="admin-login"><form action={login}><Image src="/logo-new.png" alt="Credwisory" width={180} height={46} priority/><h1>Admin console</h1><p>Secure team access</p><label>Email<input name="email" type="email" autoComplete="email" required /></label><label>Password<input name="password" type="password" autoComplete="current-password" minLength={12} required /></label>{error && <p role="alert">{error}</p>}<button disabled={busy}>{busy ? "Signing in…" : "Sign in"}</button></form></main>; }
