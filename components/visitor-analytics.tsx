"use client";

/** Visitor monitoring is intentionally disabled. Explicit form submissions
 * continue to use their own POST flow and are not affected. */
export function VisitorAnalytics() {
  return null;
}
